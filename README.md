# HTML-authentication
A basic authentication flow built using only HTML. It contains login, register, forgot password, reset password, and dashboard pages with proper redirections using anchor tags. No CSS or JavaScript is used.
